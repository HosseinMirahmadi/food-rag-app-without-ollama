
---

# 🥗 دستیار هوشمند غذا و رستوران (نسخه موبایل / آنلاین)

این پروژه یک **دستیار هوشمند مبتنی بر RAG (Retrieval-Augmented Generation)** است که به‌صورت **آنلاین و قابل اجرا روی موبایل** پیاده‌سازی شده و بدون نیاز به اجرای LLM محلی (Ollama) کار می‌کند.

این نسخه برای اجرا در محیط‌های محدود (مانند موبایل) طراحی شده و از **HuggingFace API** برای تولید پاسخ استفاده می‌کند.

---

## ✨ ویژگی‌ها

- 🌐 دریافت اطلاعات از لینک‌های وب (مقالات، منوها، منابع غذایی)
- 📚 ساخت پایگاه دانش برداری (Vector DB) با Chroma
- 🔍 جستجوی معنایی با Embedding چندزبانه
- 🤖 تولید پاسخ هوشمند با مدل زبانی از طریق HuggingFace API
- 📱 قابل اجرا روی موبایل و دسکتاپ
- ☁️ اجرای آنلاین از طریق Streamlit Cloud
- 🇮🇷 پشتیبانی کامل از زبان فارسی (RTL)

---

## 🧠 معماری پروژه (RAG)

جریان کلی پردازش در این پروژه به شکل زیر است:

1. دریافت داده از وب با `WebBaseLoader`
2. تقسیم متن‌ها با `RecursiveCharacterTextSplitter`
3. تبدیل متن‌ها به بردار با مدل Embedding چندزبانه
4. ذخیره‌سازی بردارها در پایگاه داده Chroma
5. بازیابی اسناد مرتبط با سوال کاربر
6. تولید پاسخ نهایی توسط مدل زبانی (HuggingFace API)

> منطق RAG در این نسخه به‌صورت کامل پیاده‌سازی شده است.

---

## 🛠️ تکنولوژی‌های استفاده شده

- Python
- Streamlit
- LangChain
- HuggingFace Hub (API)
- Chroma Vector Store
- Sentence Transformers
- Pandas

---

## ☁️ نحوه اجرا (نسخه آنلاین)

این پروژه از طریق **Streamlit Cloud** اجرا می‌شود.

- کد کامل پروژه در این ریپوزیتوری GitHub قرار دارد
- Streamlit Cloud به این ریپو متصل است
- با هر بار اجرا، کدها مستقیماً از GitHub لود و اجرا می‌شوند
- هیچ کدی به‌صورت محلی روی موبایل اجرا نمی‌شود

🔗 **لینک اجرای آنلاین (Live Demo):**  
https://food-rag-app-without-ollama-hchcnmiqq69bitd6rrxsew.streamlit.app/

---

## ⚠️ نکته مهم در اجرای آنلاین

در برخی موارد، قبل از تست پروژه ممکن است نیاز باشد برنامه از طریق محیط **Streamlit Cloud Restart (Reboot)** شود تا اپلیکیشن به‌درستی بارگذاری گردد.

---

## 🔑 تنظیم توکن HuggingFace (برای اجرای محلی)

در صورت اجرای محلی پروژه، باید توکن HuggingFace را در مسیر زیر قرار دهید:

.streamlit/secrets.toml

و داخل آن بنویسید:

```toml
HUGGINGFACEHUB_API_TOKEN = "YOUR_API_TOKEN"


---

📁 ساختار پروژه

├── streamlit_app.py
├── requirements.txt
├── README.md
└── .streamlit/
    └── secrets.toml


---